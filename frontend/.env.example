# Only needed for a production build where the frontend and backend are on
# different domains (e.g. Vercel + Railway). Leave unset for local dev -
# Vite's dev server proxies /api to localhost:8000 instead (see vite.config.ts).
VITE_API_BASE_URL="https://your-backend.up.railway.app"
